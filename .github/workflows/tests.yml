name: Tests

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev,vis,bench]"

      - name: Run Ruff
        run: python -m ruff check .

      - name: Run Black check
        run: python -m black --check .

      - name: Generate demo data
        run: python examples/generate_demo_data.py --output "$RUNNER_TEMP/demo_data"

      - name: Run portfolio pipeline smoke test
        run: |
          python -m pointcloud_geolab pipeline \
            --input "$RUNNER_TEMP/demo_data" \
            --output "$RUNNER_TEMP/portfolio_demo" \
            --min-points 5

      - name: Run tests with coverage
        run: python -m pytest --cov=pointcloud_geolab
